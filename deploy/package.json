{
  "name": "hardhat-project",
  "scripts": {
    "deploy": "npx hardhat ignition deploy ./ignition/modules/Lock.js --network sepolia",
    "verify": "npx hardhat ignition deploy ignition/modules/Lock.js --network sepolia --verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.7"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
